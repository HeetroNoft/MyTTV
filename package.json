{
  "name": "myttv",
  "version": "0.2.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && zip -r dist/myttv-beta.zip . -x 'node_modules/*' -x '.git/*' -x 'dist/*' -x '*.DS_Store'"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "amazon-ivs-player": "1.34.1"
  }
}
